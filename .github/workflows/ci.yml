name: CI
on:
  pull_request:
    branches: [develop]

jobs:
  build:
    runs_on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v1
        with:
          node-version: "12.x"
      - run: npm ci


